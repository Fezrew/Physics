#include "Rope.h"
#include "Spring.h"

void Rope::Build(PhysicsScene* scene, vec2 position, float spacing, float springForce, float damping, int length, vec4 colour)
{
	for (int i = 0; i < length; i++)
	{
		//spheres.push_back();
	}

	// second pass - add springs in
	
}